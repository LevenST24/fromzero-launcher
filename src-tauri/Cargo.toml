[package]
name = "fromzero-launcher"
version = "0.2.7"
description = "FromZero Launcher — Windows quick launcher with liquid-glass UI, app/file search and previews"
authors = ["LevenST"]
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[lib]
# The `_lib` suffix may seem redundant but it is necessary
# to make the lib name unique and wouldn't conflict with the bin name.
# This seems to be only an issue on Windows, see https://github.com/rust-lang/cargo/issues/8519
name = "fromzero_launcher_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = ["protocol-asset", "tray-icon"] }
tauri-plugin-autostart = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
pinyin = "0.10"
open = "5"
winapi = { version = "0.3.9", features = ["libloaderapi", "fileapi"] }
tokio = { version = "1", features = ["rt-multi-thread"] }
windows-capture = "1"
zip = { version = "8.6.0", default-features = false, features = ["deflate"] }
# List-only 7z preview; defaults pull compress/util we don't need for headers
sevenz-rust2 = { version = "0.21.3", default-features = false }

[target.'cfg(not(any(target_os = "android", target_os = "ios")))'.dependencies]
tauri-plugin-global-shortcut = "2"
tauri-plugin-single-instance = "2"

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
strip = true
panic = "abort"
